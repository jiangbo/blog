# 1180-怪物战争-单位升级系统

## 目标

实现玩家单位升级功能，消耗 COST 提升等级并重算属性。

## 环境

- Time 2026-05-02
- Zig 0.15.1

## 参考

1. <https://www.bilibili.com/video/BV171qQBPE7L/>

## 想法

选中单位可以升级，我把升级的快捷键取消了，暂时不用快捷键。

## component.zig

特效枚举新增 `levelUp`。

```zig
pub const EffectEnum = enum { heal, active, ready, levelUp };
```

## spawn.zig

属性计算公式提取为独立函数，升级和出击费用计算共用。

```zig
pub fn statModify(base: f32, level: f32, rarity: f32) f32 {
    return base * (0.95 + 0.05 * level) * (0.9 + 0.1 * rarity);
}
```

升级时从模板重新计算属性，避免在原值上反复乘倍率导致浮点漂移。

```zig
pub fn upgradeUnit(reg: *Registry, entity: Entity) void {
    const playerEnum = reg.get(entity, com.PlayerEnum);
    const template = &playerZon[@intFromEnum(playerEnum)];

    const stats = reg.getPtr(entity, com.Stats);
    stats.level += 1;
    const level = stats.level;
    const rarity = stats.rarity;

    stats.maxHealth = statModify(template.stats.maxHealth, level, rarity);
    stats.health = stats.maxHealth;
    stats.attack = statModify(template.stats.attack, level, rarity);
    stats.defense = statModify(template.stats.defense, level, rarity);
    stats.range = statModify(template.stats.range, level, rarity);
    stats.interval = statModify(template.stats.interval, level, rarity);

    const position = reg.get(entity, com.Position);
    const effectEntity = effect(reg, .levelUp);
    reg.add(effectEntity, position);
    reg.add(effectEntity, com.DeadOnFinish{});
}
```

## effect.zon

新增升级特效，使用 `level_up.png`，每帧 128x128，共 12 帧。

## gui.zig

选中玩家单位时显示升级按钮，COST 不足时按钮禁用。

```zig
fn renderSelectedUpgrade(reg: *zhu.ecs.Registry, entity: zhu.ecs.Entity) void {
    if (!reg.has(entity, com.Player)) return;

    const playerEnum = reg.get(entity, com.PlayerEnum);
    const upgradeCost = spawn.playerZon[@intFromEnum(playerEnum)].cost;

    gui.igBeginDisabled(ctx.cost < upgradeCost);
    const clicked = gui.igButton("升级");
    gui.igEndDisabled();
    gui.igSameLine();
    _ = gui.igText("消耗 %.0f COST", upgradeCost);

    if (clicked and ctx.cost >= upgradeCost) {
        ctx.cost -= upgradeCost;
        spawn.upgradeUnit(reg, entity);
    }
}
```

## 效果

选中玩家单位点击升级按钮后，等级提升，属性按公式重算，头顶显示升级特效。

![单位升级系统][1]

[1]: images/怪物战争36.png

## 附录
